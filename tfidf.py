import json
import sys
import os
import read_json
import pickle

def calculate_tfidf(data):
    '''
    '''
    #num_reviews = len(data)
    #term_freq = dict()
    #review_dict = dict()
    #for review in data:
    #    s = review['reviewText'].lower()  # grab the review text
    #    tokens = word_tokenize(s)  # grab the words
    #    for token in tokens:
    #        review_dict[review[]]
    


#d = read_json('reviews_Musical_Instruments_5.json')
#print(d[1])
